# --- Telegram bot stack (v13.x, sync) ---
python-telegram-bot==13.15
APScheduler==3.6.3
tornado==6.1          # PTB 13 requires >=6.1; 6.1 is the safest pin

# --- Web server / webhook ---
Flask==3.0.3
gunicorn==21.2.0

# --- Time & tz helpers (used by APScheduler and our code) ---
pytz==2024.1
tzlocal==2.1          # APScheduler 3.6.x works best with tzlocal 2.x
six==1.16.0

# --- Data / math ---
pandas==2.2.2
numpy==1.26.4

# --- HTTP ---
requests==2.32.3

# --- Env vars ---
python-dotenv==1.0.1

# --- Flask deps (explicit pins for stability) ---
Jinja2==3.1.4
Werkzeug==3.0.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.8.2